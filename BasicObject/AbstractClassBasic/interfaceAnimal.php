<?php
/*
 * brief create interface with smt in common just to see is it works or not
 * 
 */
interface AnimalInterface
{
	/**
	 * @ move everything that is this object;
	 * 
	 * @param	int;
	 */
	public function move( $time );
	
}