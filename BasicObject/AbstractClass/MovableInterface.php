<?php

interface MovableInterface
{
		/**
		 * 
		 * @brief		Shows that every objects getDistance by $time
		 * which is used in the abstract class.
		 * 
		 * @param $time	int;
		 */
	public function getDistance( $time );
}